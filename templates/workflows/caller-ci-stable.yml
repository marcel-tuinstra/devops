name: CI (stable channel example)

on:
  pull_request:
  workflow_dispatch:

jobs:
  ci:
    uses: marcel-tuinstra/devops/.github/workflows/reusable-ci.yml@v1
    with:
      node-version: "20"
      workdir: "."
      test-command: npm test
