name: Reusable CD PHP

on:
  workflow_call:
    secrets:
      SSH_PRIVATE_KEY:
        description: SSH private key for deployment host authentication
        required: false
    inputs:
      service-name:
        description: Docker Compose PHP service name on target host
        required: true
        type: string
      nginx-service-name:
        description: Docker Compose nginx service name on target host
        required: false
        default: "nginx"
        type: string
      image-name:
        description: Full GHCR image name without tag (used for both php and nginx images)
        required: true
        type: string
      health-url:
        description: "Public health URL (optional, informational only - health check runs via SSH on localhost)"
        required: false
        default: ""
        type: string
      health-path:
        description: "Path checked on localhost during health check"
        required: false
        default: "/health"
        type: string
      health-timeout-seconds:
        description: "Maximum seconds to wait for health check"
        required: false
        default: 180
        type: number
      port:
        description: Exposed nginx service port (internal container port)
        required: false
        default: 80
        type: number
      environment:
        description: GitHub Environment name (staging or production)
        required: true
        type: string
      workdir:
        description: Project working directory
        required: false
        default: .
        type: string
      dockerfile:
        description: Dockerfile path relative to consumer repo root
        required: false
        default: Dockerfile
        type: string
      remote-path:
        description: Deploy path on target host containing compose file
        required: true
        type: string
      ssh-user:
        description: SSH user for deploy host
        required: true
        type: string
      ssh-port:
        description: SSH port
        required: false
        default: 22
        type: number
      compose-file:
        description: Compose file path relative to consumer repo root
        required: false
        default: docker-compose.yml
        type: string
      build-args:
        description: "Newline-separated Docker build args (e.g. ARG_NAME=value)"
        required: false
        default: ""
        type: string
      trivy-severity:
        description: "Comma-separated severity levels to scan for"
        required: false
        default: "CRITICAL,HIGH"
        type: string
      trivy-exit-code:
        description: "Exit code when vulnerabilities are found (1 = fail, 0 = warn only)"
        required: false
        default: "1"
        type: string
      upload-sarif:
        description: "Upload Trivy SARIF results to GitHub code scanning"
        required: false
        default: false
        type: boolean
      run-migrations:
        description: "Run Doctrine migrations after deploy"
        required: false
        default: false
        type: boolean
      restart-worker:
        description: "Restart messenger worker service after deploy"
        required: false
        default: false
        type: boolean
      worker-services:
        description: "Comma-separated list of worker services to pin to PHP image (empty = no workers)"
        required: false
        default: ""
        type: string
      pre-deploy-cleanup:
        description: "Optional cleanup command before deployment (none or down-remove-orphans)"
        required: false
        default: "none"
        type: string
      immutable-runtime:
        description: "Enforce immutable runtime guardrails (no /var/www/html bind mounts on deployed services)"
        required: false
        default: false
        type: boolean
      verify-runtime-command:
        description: "Optional command executed inside deployed PHP service (for runtime verification)"
        required: false
        default: ""
        type: string
      verify-runtime-command-contains:
        description: "Optional substring expected in verify-runtime-command output"
        required: false
        default: ""
        type: string

jobs:
  build-scan-push:
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      packages: write
      security-events: write
    defaults:
      run:
        working-directory: ${{ inputs.workdir }}
    outputs:
      php-image-ref: ${{ steps.php-image.outputs.image_ref }}
      nginx-image-ref: ${{ steps.nginx-image.outputs.image_ref }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.0

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e # v4.0.0

      - name: Login to GHCR
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f # v4.0.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      # Build PHP image
      - name: Build PHP image for scanning
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.0.0
        with:
          context: ${{ inputs.workdir }}
          file: ${{ inputs.dockerfile }}
          target: php_prod
          push: false
          load: true
          tags: scan-target-php:${{ github.sha }}
          build-args: ${{ inputs.build-args }}

      - name: Run Trivy on PHP image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: scan-target-php:${{ github.sha }}
          format: table
          exit-code: ${{ inputs.trivy-exit-code }}
          severity: ${{ inputs.trivy-severity }}
          ignore-unfixed: true
          cache: false

      - name: Upload Trivy PHP scan results
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        if: ${{ always() && inputs.upload-sarif }}
        with:
          image-ref: scan-target-php:${{ github.sha }}
          format: sarif
          output: trivy-php-results.sarif
          severity: ${{ inputs.trivy-severity }}
          ignore-unfixed: true
          cache: false

      - name: Upload PHP SARIF file
        uses: github/codeql-action/upload-sarif@eec0bff2f6c15bf3f1e8a0152f94d17664a06a06 # v4.32.6
        if: ${{ always() && inputs.upload-sarif && hashFiles('trivy-php-results.sarif') != '' }}
        continue-on-error: true
        with:
          sarif_file: trivy-php-results.sarif
          category: trivy-php

      - name: Push PHP image to GHCR
        id: php-push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.0.0
        with:
          context: ${{ inputs.workdir }}
          file: ${{ inputs.dockerfile }}
          target: php_prod
          push: true
          tags: |
            ${{ inputs.image-name }}:${{ github.sha }}
            ${{ inputs.image-name }}:latest
          build-args: ${{ inputs.build-args }}

      - name: Prepare PHP image reference
        id: php-image
        run: |
          digest="${{ steps.php-push.outputs.digest }}"
          image_ref="${{ inputs.image-name }}@${digest}"
          echo "image_ref=${image_ref}" >> "$GITHUB_OUTPUT"

      # Build nginx image
      - name: Build nginx image
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.0.0
        with:
          context: ${{ inputs.workdir }}
          file: ${{ inputs.dockerfile }}
          target: nginx_prod
          push: false
          load: true
          tags: scan-target-nginx:${{ github.sha }}

      - name: Run Trivy on nginx image
        uses: aquasecurity/trivy-action@ed142fd0673e97e23eac54620cfb913e5ce36c25 # v0.36.0
        with:
          image-ref: scan-target-nginx:${{ github.sha }}
          format: table
          exit-code: ${{ inputs.trivy-exit-code }}
          severity: ${{ inputs.trivy-severity }}
          ignore-unfixed: true
          cache: false

      - name: Push nginx image to GHCR
        id: nginx-push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a # v7.0.0
        with:
          context: ${{ inputs.workdir }}
          file: ${{ inputs.dockerfile }}
          target: nginx_prod
          push: true
          tags: |
            ${{ inputs.image-name }}:nginx-${{ github.sha }}
            ${{ inputs.image-name }}:nginx-latest
          build-args: ${{ inputs.build-args }}

      - name: Prepare nginx image reference
        id: nginx-image
        run: |
          digest="${{ steps.nginx-push.outputs.digest }}"
          image_ref="${{ inputs.image-name }}@${digest}"
          echo "image_ref=${image_ref}" >> "$GITHUB_OUTPUT"

  deploy:
    runs-on: ubuntu-latest
    needs: build-scan-push
    environment: ${{ inputs.environment }}
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.0

      - name: Ensure SSH key is present
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}
        run: |
          if [ -z "$SSH_PRIVATE_KEY" ]; then
            echo "Missing SSH_PRIVATE_KEY. Configure it as an environment secret in the caller repo, or pass it explicitly when calling this workflow." >&2
            exit 1
          fi

      - name: Configure SSH
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          ssh-keyscan -p "${{ inputs.ssh-port }}" "${{ vars.SSH_HOST }}" >> ~/.ssh/known_hosts

      - name: Upload compose file
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "mkdir -p '${{ inputs.remote-path }}'"
          scp -P "${{ inputs.ssh-port }}" \
            "${{ inputs.compose-file }}" \
            "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}:${{ inputs.remote-path }}/docker-compose.yml"

      - name: Deploy services over SSH
        env:
          WORKER_SERVICES: ${{ inputs.worker-services }}
          PHP_IMAGE_REF: ${{ needs.build-scan-push.outputs.php-image-ref }}
          NGINX_IMAGE_REF: ${{ needs.build-scan-push.outputs.nginx-image-ref }}
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "WORKER_SERVICES='${WORKER_SERVICES}' PHP_IMAGE_REF='${PHP_IMAGE_REF}' NGINX_IMAGE_REF='${NGINX_IMAGE_REF}' bash -s" <<'EOF'
          set -euo pipefail
          cd "${{ inputs.remote-path }}"
          override_file=".deploy-override-${{ github.run_id }}-${{ github.run_attempt }}-${{ inputs.service-name }}.yml"
          trap 'rm -f "$override_file"' EXIT

          # Create override file with pinned image digests
          cat > "$override_file" <<OVERRIDE
          services:
            ${{ inputs.service-name }}:
              image: ${PHP_IMAGE_REF}
            ${{ inputs.nginx-service-name }}:
              image: ${NGINX_IMAGE_REF}
          OVERRIDE

          # Add worker services if specified
          if [ -n "$WORKER_SERVICES" ]; then
            IFS=',' read -ra WORKERS <<< "$WORKER_SERVICES"
            for worker in "${WORKERS[@]}"; do
              worker=$(echo "$worker" | xargs)  # trim whitespace
              cat >> "$override_file" <<WORKER
            ${worker}:
              image: ${PHP_IMAGE_REF}
          WORKER
            done
          fi

          if [ "${{ inputs.pre-deploy-cleanup }}" = "down-remove-orphans" ]; then
            docker compose -f docker-compose.yml -f "$override_file" down --remove-orphans || true
          fi

          deploy_services=("${{ inputs.service-name }}" "${{ inputs.nginx-service-name }}")

          if [ -n "$WORKER_SERVICES" ]; then
            IFS=',' read -ra WORKERS <<< "$WORKER_SERVICES"
            for worker in "${WORKERS[@]}"; do
              worker=$(echo "$worker" | xargs)
              if [ -n "$worker" ]; then
                deploy_services+=("$worker")
              fi
            done
          fi

          # Pull and deploy only relevant services.
          docker compose -f docker-compose.yml -f "$override_file" pull "${deploy_services[@]}"
          docker compose -f docker-compose.yml -f "$override_file" up -d "${deploy_services[@]}"
          EOF

      - name: Verify deployed runtime image and mount guardrails
        env:
          WORKER_SERVICES: ${{ inputs.worker-services }}
          PHP_IMAGE_REF: ${{ needs.build-scan-push.outputs.php-image-ref }}
          NGINX_IMAGE_REF: ${{ needs.build-scan-push.outputs.nginx-image-ref }}
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "WORKER_SERVICES='${WORKER_SERVICES}' PHP_IMAGE_REF='${PHP_IMAGE_REF}' NGINX_IMAGE_REF='${NGINX_IMAGE_REF}' IMMUTABLE_RUNTIME='${{ inputs.immutable-runtime }}' bash -s" <<'EOF'
          set -euo pipefail
          cd "${{ inputs.remote-path }}"

          check_service() {
            local service="$1"
            local expected_image="$2"

            local container_id
            container_id=$(docker compose -f docker-compose.yml ps -q "$service")
            if [ -z "$container_id" ]; then
              echo "Service '$service' has no running container" >&2
              exit 1
            fi

            local actual_image
            actual_image=$(docker inspect "$container_id" --format '{{.Config.Image}}')
            if [ "$actual_image" != "$expected_image" ]; then
              echo "Service '$service' image mismatch. Expected '$expected_image' got '$actual_image'" >&2
              exit 1
            fi

            if [ "$IMMUTABLE_RUNTIME" = "true" ]; then
              local has_code_bind
              has_code_bind=$(docker inspect "$container_id" --format '{{range .Mounts}}{{if and (eq .Type "bind") (eq .Destination "/var/www/html")}}yes{{end}}{{end}}')
              if [ "$has_code_bind" = "yes" ]; then
                echo "Service '$service' has forbidden bind mount on /var/www/html" >&2
                exit 1
              fi
            fi
          }

          check_service "${{ inputs.service-name }}" "$PHP_IMAGE_REF"
          check_service "${{ inputs.nginx-service-name }}" "$NGINX_IMAGE_REF"

          if [ -n "$WORKER_SERVICES" ]; then
            IFS=',' read -ra WORKERS <<< "$WORKER_SERVICES"
            for worker in "${WORKERS[@]}"; do
              worker=$(echo "$worker" | xargs)
              if [ -n "$worker" ]; then
                check_service "$worker" "$PHP_IMAGE_REF"
              fi
            done
          fi
          EOF

      - name: Run database migrations
        if: ${{ inputs.run-migrations }}
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" <<'EOF'
          set -euo pipefail
          cd "${{ inputs.remote-path }}"

          # Wait for PHP container to be ready
          sleep 5

          # Run migrations
          docker compose -f docker-compose.yml exec -T ${{ inputs.service-name }} \
            php bin/console doctrine:migrations:migrate --no-interaction --allow-no-migration
          EOF

      - name: Restart worker services
        if: ${{ inputs.restart-worker && inputs.worker-services != '' }}
        env:
          WORKER_SERVICES: ${{ inputs.worker-services }}
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "WORKER_SERVICES='${WORKER_SERVICES}' bash -s" <<'EOF'
          set -euo pipefail
          cd "${{ inputs.remote-path }}"
          # Convert comma-separated to space-separated for docker compose
          services="${WORKER_SERVICES//,/ }"
          docker compose -f docker-compose.yml restart $services
          EOF

      - name: Verify runtime command
        if: ${{ inputs.verify-runtime-command != '' }}
        env:
          VERIFY_RUNTIME_COMMAND: ${{ inputs.verify-runtime-command }}
          VERIFY_RUNTIME_COMMAND_CONTAINS: ${{ inputs.verify-runtime-command-contains }}
        run: |
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "VERIFY_RUNTIME_COMMAND='${VERIFY_RUNTIME_COMMAND}' VERIFY_RUNTIME_COMMAND_CONTAINS='${VERIFY_RUNTIME_COMMAND_CONTAINS}' bash -s" <<'EOF'
          set -euo pipefail
          cd "${{ inputs.remote-path }}"

          output=$(docker compose -f docker-compose.yml exec -T "${{ inputs.service-name }}" sh -lc "$VERIFY_RUNTIME_COMMAND")
          printf '%s\n' "$output"

          if [ -n "$VERIFY_RUNTIME_COMMAND_CONTAINS" ]; then
            case "$output" in
              *"$VERIFY_RUNTIME_COMMAND_CONTAINS"*) ;;
              *)
                echo "Runtime verification output did not contain expected token: $VERIFY_RUNTIME_COMMAND_CONTAINS" >&2
                exit 1
                ;;
            esac
          fi
          EOF

      - name: Health check
        run: |
          timeout=${{ inputs.health-timeout-seconds }}
          interval=5
          deadline=$(( $(date +%s) + timeout ))

          # Resolve the host port from the running container
          if ! mapped=$(ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "cd '${{ inputs.remote-path }}' && docker compose -f docker-compose.yml port '${{ inputs.nginx-service-name }}' ${{ inputs.port }}"); then
            echo "Unable to resolve mapped port for service '${{ inputs.nginx-service-name }}' on internal port '${{ inputs.port }}'" >&2
            exit 1
          fi
          host_port=$(echo "$mapped" | cut -d: -f2)
          echo "Container mapped to localhost:${host_port}"

          while [[ $(date +%s) -lt $deadline ]]; do
            if ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
              "curl -fsS --connect-timeout 2 --max-time 5 http://localhost:${host_port}${{ inputs.health-path }}" >/dev/null 2>&1; then
              echo "Health check passed on localhost:${host_port}${{ inputs.health-path }}"
              exit 0
            fi
            sleep "$interval"
          done

          echo "Health check failed on localhost:${host_port}${{ inputs.health-path }}" >&2
          echo "Last response (headers + first body bytes):" >&2
          ssh -p "${{ inputs.ssh-port }}" "${{ inputs.ssh-user }}@${{ vars.SSH_HOST }}" \
            "curl -iS --connect-timeout 2 --max-time 10 http://localhost:${host_port}${{ inputs.health-path }} || true" >&2
          exit 1
